$(function() {
  $(".datepicker").datepicker({
    autoclose: true,
    todayHighlight: true
  });

  $(".datepicker").datepicker({
    format: "mm/dd/yyyy",
    todayHighlight: true,
    autoclose: true
  });
});
